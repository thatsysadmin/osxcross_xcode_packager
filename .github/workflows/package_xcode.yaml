name: Package Xcode
run-name: Package Xcode for osxcross
on: [push]
jobs:
  Prepare:
    runs-on: macos-13
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Get information on runner
        run: $GITHUB_WORKSPACE/get_runner_info.sh